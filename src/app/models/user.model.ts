export interface User {
    id: number;
    name: string;
    age: number;
    email: string;
    password: string;
    role: string;
    refreshToken: string;
  }
export class User implements User {
    constructor(     
      
    ) {}
}
  